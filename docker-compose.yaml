services:
  web:
    build:
      context: .
      network: host
    container_name: dug 
    ports:
      - "8501:8501"
    volumes:
      - .:/app      # THIS IS THE MAGIC LINE FOR LIVE RELOAD
    restart: unless-stopped
