services:
  web:
<<<<<<< HEAD
    build: .
=======
    build:
      context: .
      network: host # Fixed: 'network' inside build is different than service network
>>>>>>> ece459571d59d792d09bc5c6d03ef1d8d0ac7fdb
    container_name: dug 
    ports:
      - "8501:8501"
    stdin_open: true 
    tty: true 
    environment:
      - PYTHONUNBUFFERED=1    
    env_file:
      - .env
    volumes:
<<<<<<< HEAD
      - .:/app
    working_dir: /app
    # Pointing to src/main.py as per your previous command
    command: streamlit run src/main.py --server.address=0.0.0.0 --server.port=8501
=======
      - .:/app       
>>>>>>> ece459571d59d792d09bc5c6d03ef1d8d0ac7fdb
    restart: unless-stopped

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: mydatabase
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U myuser -d mydatabase"]
      interval: 5s
      timeout: 5s
      retries: 5

  tester:
    build: 
      context: .
      network: host
    
    environment:
      - DATABASE_URL=postgresql+psycopg://myuser:mypassword@db:5432/mydatabase
      - PYTHONPATH=/app
    working_dir: /app
    volumes:
          - .:/app       

    depends_on:
      db:
        condition: service_healthy
    command: python3 -m unittest tests/databasetest.py

volumes:
  pgdata:
    driver: local
